<!-- spaex-composed:source_hash="5141532044bd735bd09431e9113fc5e6985887887052eeb1b26e545217e4bde6" build_input_hash="af2980b398313f9fd776b0fbdde915f784cc47a246cc19e3c830d50e891cc668" version="1" -->
# spaex Behavior Harness

_This file is generated by `spaex install`. Do not edit by hand._
_Change fragments in `.spaex/constitution.d/` and re-run install._

## MUST

- Add type hints to public functions, methods, and meaningful data structures, preferring precise types and protocols over `Any` (using `Any` only at a deliberate untyped boundary and narrowing the value before it reaches domain logic), and preferring modern built-in generics such as `list[str]` and `dict[str, int]` and abstract collection types such as `Mapping` for read-only inputs when they describe the contract better. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Before handing off a change, run the project's formatter, linter, type checker, and relevant tests — or, absent project-specific commands, a baseline of `python -m compileall`, the configured test command, and the configured static checks — and report what was run and any checks that were unavailable or skipped. _[from `com.github.haexmas.atoms.coding-guideline-python/python`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Follow the repository's supported Python versions and style configuration, using the configured formatter, linter, type checker, and test runner instead of introducing competing project-wide tooling. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- For distributable projects, use `pyproject.toml` for build-system metadata, project metadata, dependencies, and tool configuration, keeping the declared Python version and dependency constraints explicit and using the project's chosen environment or lock workflow consistently. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Keep async code non-blocking, using an appropriate async API or explicitly moving blocking or CPU-heavy work to a worker instead of calling it directly from an event loop. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Model data explicitly, using a small `dataclass`, `Enum`, or dedicated value object when a concept has invariants. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Raise specific exceptions, catch only exceptions that can be handled at that layer, and preserve the original cause with exception chaining when translating errors. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Use context managers for resources such as files, locks, and transactions. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Validate external input at the boundary and convert it to a domain shape once. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Exercise successful and failure behavior, especially malformed input, missing configuration, expected exceptions, authorization decisions, persistence failures, and resource cleanup, asserting the exception type and meaningful details without over-specifying incidental message formatting. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- For async code, test coroutines with the project's supported async test tooling, and test cancellation, timeout, cleanup, and ordering assumptions when they affect correctness. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Keep each test independent and deterministic, using temporary directories, controlled clocks, seeded randomness, and explicit fakes or mocks at external boundaries. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Keep mocks narrow and purposeful, mocking an external boundary when isolation or determinism requires it. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Keep parametrized cases focused and readable, adding a new case when it demonstrates a distinct boundary or regression. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Run the project's normal test and static-check commands, making sure at minimum that the test runner discovers the intended files and that a deliberately broken assertion makes the test fail during test development. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Test observable behavior and public contracts, not private implementation details — a refactor that preserves the contract should not require rewriting the tests, and a test is useful only when it can fail for a real regression. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Use `unittest` or the project's chosen test framework consistently, with fixtures that make setup and cleanup explicit via context managers and framework cleanup hooks, so a failing test cannot leak files, connections, processes, or patches into the next test. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Use focused unit tests for pure logic and isolated components, integration tests for real module boundaries, and end-to-end tests only for workflows that need the complete system, keeping the test level visible in the test location and name according to the repository convention. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Add or update tests with behavior changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Before the first edit, verify the repository root, the active branch, and the registered worktrees, and create a suitable worktree before modifying files if none exists. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- For any existing Git repository, author all changes — source code, tests, documentation, configuration, and generated files that are part of the change — in a dedicated repository-local worktree (a directory registered for the target repository by `git worktree list`, not a copied directory or unrelated clone) on a topic branch. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Give each function one coherent responsibility at one level of abstraction, extracting a helper when it gives a meaningful name to a concept, makes a branch independently testable, or removes repeated logic — not merely because the function crossed an arbitrary line count. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep dependencies directed and understandable, putting I/O, framework adapters, and process wiring at the edges when the architecture calls for such a separation. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep each change self-contained and easy to review: production code, relevant tests, documentation, and configuration belong together when they describe the same behavior. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep each task isolated in its own worktree and topic branch so that changes can be reviewed, tested, committed, and removed independently. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep public module interfaces narrow and intentional, hiding implementation details behind the smallest stable API the callers need. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep related tests close to the behavior they verify according to the language and repository convention, and update its tests, documentation, fixtures, and call sites as part of a module split rather than leaving them misleadingly behind. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep side effects at visible boundaries. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Maintain test code — unit, integration, contract, and end-to-end tests, including test bodies and test-only helpers — in dedicated test files separate from production code in every language, following the repository's convention for test-file placement and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Name code after its domain meaning and behavior, since a clear name is better than a comment that explains an otherwise opaque `data`, `helper`, or `process` function. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Organize code around cohesive domain or feature boundaries where the codebase supports it. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat 500 LoC as the upper boundary for hand-maintained source files: a file clearly over 500 LoC must be split, or have an explicit, documented reason to remain large together with a concrete plan for a later split. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- When a repository already has a documented structure, naming scheme, or size convention, follow it unless the task explicitly changes that convention. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Before authoring any new named function, class, component, store, module, or CLI command, agents must consult the project's `graphify` knowledge graph via `graphify query`/`path`/`explain` and prefer extending an existing matching candidate over authoring a parallel implementation, escalating borderline matches to the operator instead of deciding alone. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- Rigor must not be relaxed for understanding the problem, input validation at trust boundaries, error handling that prevents data loss, security, accessibility, real-hardware calibration, or anything explicitly requested, and any non-trivial logic change must leave behind one runnable check written with the project's existing test tooling. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## MUST_NOT

- Call blocking I/O or CPU-heavy work directly from an event loop, or call `asyncio.run()` from code that may already be running inside an event loop. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Claim checks passed, or treat a green-looking diff as verified, without actually running the project's formatter, linter, type checker, and relevant tests. _[from `com.github.haexmas.atoms.coding-guideline-python/python`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Let resource cleanup depend on reference counting, garbage collection, or a caller remembering a separate close operation. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Pass a growing collection of positional tuples or unrelated dictionary keys through the application instead of an explicit data model. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Use a bare `except`, catch `Exception` to hide a failure, or return `None` as an undocumented substitute for an error. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Call `asyncio.run()` inside an already-running event loop, or use sleeps to wait for concurrency, in tests. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Hide unrelated scenarios inside a large fixture or broad parameter matrix. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Mock the code under test so heavily that the test only proves its own setup. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Abstract two things merely because they look similar; establish that they have the same behavior, ownership, change reason, and invariants first. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Add artificial wrappers, blank lines, or extra files only to satisfy a line-count threshold. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Author changes in the primary checkout; reserve it for inspection and worktree management (read-only inspection of any checkout is fine). _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Bundle independent refactors and unrelated cleanups into the same change as the behavior it describes; split them into separate changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Create folders only for superficial symmetry, such as putting every small class, interface, or helper in its own folder. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Enforce a universal maximum line count for functions, files, or pull requests; weigh domain complexity, cohesion, control flow, and the number of reasons to change instead. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Leave references to the LLM agent itself — its model, runtime, role, prompts, or participation — in project artifacts, including commit messages, commit trailers such as `Co-authored-by`, source and documentation comments, changelogs, generated content, metadata, and other repository content. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Place inline test modules or test functions in production files. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Scatter one simple operation across many microscopic functions. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Split a file solely to make it shorter. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Use a documented structure, naming scheme, or size convention as a reason to reformat or reorganize an entire codebase. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Use comments to narrate code that could be made clear through structure and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- A graphify context-map lookup must not cause a full graph or full repository dump to be copied into the agent's context. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-context-map`]_

## SHOULD

- At an application's top-level entry point, use `asyncio.run()`; library code or code already inside an event loop should expose or await a coroutine instead of starting a nested event loop. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Prefer simple, readable control flow: use comprehensions for small transformations and a normal loop when conditions, side effects, or error handling would make the comprehension difficult to read. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Prefer the standard library and existing project dependencies before adding a new package, keeping dependency additions justified by a real capability or maintenance benefit rather than a shorter one-off expression. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Keep tests easy to read, fast enough to run frequently, and honest about what they do not cover, since a passing test is evidence only for the behavior it actually exercises. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- A file should have one primary reason to change, and be split when it has unrelated responsibilities, independent public concepts, different change owners, or tests that are difficult to isolate. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Comments should explain why a non-obvious decision exists, what invariant must be preserved, or which external constraint applies, and should be updated when the behavior changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer a change that clearly improves maintainability and correctness over theoretical perfection, making the smallest reversible change that provides evidence about the next decision when a design is uncertain. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer a small amount of local duplication over a premature generic abstraction that couples unrelated callers. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer early returns or named helpers when they make the main path easier to follow, and reduce nesting. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Pure calculations and decisions should be easy to test without a database, network, filesystem, clock, or global mutable state. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Refactor repeated behavior when the shared rule is real and the abstraction makes both callers clearer, keeping the abstraction close to the domain that owns it. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Separate broad formatting or mechanical refactors from behavior changes when possible, since mixed diffs make review, debugging, rollback, and blame less reliable. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat a change of about 100 net lines as a review-size warning and a change approaching 1000 lines as a strong prompt to split the work or agree on it in advance, identifying generated code, mechanical renames, and required migrations as legitimate exceptions. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat a function or method beyond roughly 40–60 lines, or nesting deeper than roughly three levels, as a soft threshold deserving a deliberate look at whether it is still one coherent unit — a heuristic prompting review, not an automatic failure or mandatory limit. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- For any codebase question — what something does, where it lives, how pieces relate, or how to trace a symptom back to its source — agents should run a bounded `graphify query`/`path`/`explain` lookup with an explicit token budget before a raw grep or broad file reads, then read only the smallest source slices the map points to. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-context-map`]_
- Before writing code, agents should climb the lazy-senior-dev ladder — question whether it needs to be built at all, then prefer what already exists in the codebase, the standard library, a platform feature, or an already-installed dependency, then the shortest one-line expression — only after understanding the problem and tracing the real flow end to end, and should fix a shared bug once at its root cause rather than patching each call site separately. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD_NOT

- Perform expensive work, network access, or surprising side effects at module import time, unless the project explicitly uses that pattern. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Use clever metaprogramming when ordinary functions and classes suffice. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Rely on order dependence, shared mutable module state, real network services, the current time, or arbitrary sleeps in tests. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Have circular imports, hidden global state, or a domain layer that knows about infrastructure details. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Let a generic utility module become a dumping ground. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Re-export everything or create pass-through modules that add no boundary. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Agents should not introduce speculative abstractions, avoidable new dependencies, or unrequested boilerplate, and should prefer deletion over addition, boring over clever solutions, the fewest files, and the shortest diff that still reflects real understanding of the problem. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## MAY

- Let a language-required test-module declaration or registration remain in the production file. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Let generated files, vendored code, snapshots, and other files not normally edited by hand exceed the 500 LoC boundary, though they should be excluded from this assessment. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
