<!-- spaex-composed:source_hash="9e7b48fcf6e4f38bf7b68abe77960c9f008ba12e8e074b96a44a6c6e713c810a" build_input_hash="af2980b398313f9fd776b0fbdde915f784cc47a246cc19e3c830d50e891cc668" version="1" -->
# spaex Behavior Harness

_This file is generated by `spaex install`. Do not edit by hand._
_Change fragments in `.spaex/constitution.d/` and re-run install._

## MUST

- Follow the repository's supported Python versions and style configuration, using the configured formatter, linter, type checker, and test runner instead of introducing competing project-wide tooling. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Add type hints to public functions, methods, and meaningful data structures, using `Any` only at a deliberate untyped boundary and narrowing the value before it reaches domain logic. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Model data explicitly, using a small `dataclass`, `Enum`, or dedicated value object when a concept has invariants. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Validate external input at the boundary and convert it to a domain shape once. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Raise specific exceptions, catch only exceptions that can be handled at that layer, and preserve the original cause with exception chaining when translating errors. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Use context managers for resources such as files, locks, and transactions. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Keep async code non-blocking, using an appropriate async API or explicitly moving blocking or CPU-heavy work to a worker. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- For distributable projects, use `pyproject.toml` for build-system metadata, project metadata, dependencies, and tool configuration, keeping the declared Python version and dependency constraints explicit and using the project's chosen environment or lock workflow consistently. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Before handing off a change, run the project's formatter, linter, type checker, and relevant tests, falling back to `python -m compileall`, the configured test command, and configured static checks when no project-specific commands exist, and make sure the test runner discovers the intended files and that a deliberately broken assertion makes the test fail during test development. _[from `com.github.haexmas.atoms.coding-guideline-python/python`, `com.github.haexmas.atoms.coding-guideline-python/python-testing`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Use focused unit tests for pure logic and isolated components, integration tests for real module boundaries, and end-to-end tests only for workflows that need the complete system, keeping the test level visible in the test location and name according to the repository convention. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Keep each test independent and deterministic, using temporary directories, controlled clocks, seeded randomness, and explicit fakes or mocks at external boundaries. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Exercise successful and failure behavior — especially malformed input, missing configuration, expected exceptions, authorization decisions, persistence failures, and resource cleanup — and assert the exception type and meaningful details without over-specifying incidental message formatting. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Use context managers and framework cleanup hooks so a failing test cannot leak files, connections, processes, or patches into the next test. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Keep mocks narrow and purposeful, mocking an external boundary when isolation or determinism requires it. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- For async code, test coroutines with the project's supported async test tooling, and test cancellation, timeout, cleanup, and ordering assumptions when they affect correctness. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Keep parametrized test cases focused and readable, adding a new case when it demonstrates a distinct boundary or regression. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Add or update tests with behavior changes, testing observable behavior and public contracts rather than private implementation details, since a test is useful only when it can fail for a real regression and a refactor that preserves the contract should not require rewriting it. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Use `unittest` or the project's chosen test framework consistently, writing tests with whatever framework and fixtures the project has already adopted rather than pulling in a new one just for a single check; a project with no such framework yet gets a framework-free assert-based demo or one small test file instead. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- For any existing Git repository, all changes must be authored in a dedicated repository-local worktree on a topic branch; this applies to source code, tests, documentation, configuration, and generated files that are part of the change. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Before the first edit, verify the repository root, the active branch, and the registered worktrees, creating a suitable worktree before modifying files if none exists. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep each task isolated in its own worktree and topic branch so that changes can be reviewed, tested, committed, and removed independently. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat 500 LoC as the upper boundary for hand-maintained source files; a file clearly over 500 LoC must be split or have an explicit, documented reason to remain large together with a concrete plan for a later split. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Give each function one coherent responsibility at one level of abstraction. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep side effects at visible boundaries, so pure calculations and decisions are easy to test without a database, network, filesystem, clock, or global mutable state. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Name code after its domain meaning and behavior rather than relying on a comment to explain an otherwise opaque `data`, `helper`, or `process` function. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Organize code around cohesive domain or feature boundaries where the codebase supports it. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep public module interfaces narrow and intentional, hiding implementation details behind the smallest stable API the callers need. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep dependencies directed and understandable, putting I/O, framework adapters, and process wiring at the edges when the architecture calls for such a separation. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep related tests close to the behavior they verify according to the language and repository convention; a module split is not complete if its tests, documentation, fixtures, and call sites are left misleadingly behind. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Test code must be maintained in files separate from production code in every language — for unit, integration, contract, and end-to-end tests alike — with test bodies and test-only helpers living in dedicated test files, following the repository's convention for test-file placement and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep each change self-contained and easy to review — production code, relevant tests, documentation, and configuration belong together when they describe the same behavior — and split independent refactors and unrelated cleanups into separate changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- When a repository already has a documented structure, naming scheme, or size convention, follow it unless the task explicitly changes that convention. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Extract a helper, or refactor repeated behavior into an abstraction, only when justified by real evidence — a meaningful name for a concept, real duplication removal, a shared rule that makes both callers clearer, or making a branch independently testable — not merely because a function crossed a line count or a change might be reusable; keep the abstraction small and close to the domain that owns it rather than a generic dumping ground. _[from `com.github.haexmas.atoms.general-coding/general-coding`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Before writing any code, climb the following ladder and stop at the first rung that holds: whether the code needs to be built at all (YAGNI); whether it already exists in the codebase and can be reused; whether the standard library already does it; whether a native platform feature covers it; whether an already-installed dependency solves it; whether it can be one line; and only then, write the minimum code that works. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Understand the problem before climbing the ladder: read the task and the code it touches, and trace the real flow end to end, before writing code. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- When fixing a bug, fix the root cause in the shared function rather than the symptom — grep every caller of the function you touch and fix it once there, since patching only the path the ticket names leaves a sibling caller still broken. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- When two stdlib approaches are the same size, pick the edge-case-correct option: lazy means less code, not the flimsier algorithm. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Mark deliberate simplifications that cut a real corner with a known ceiling, such as a global lock, an O(n²) scan, or a naive heuristic, with a `ponytail:` comment naming the ceiling and upgrade path. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Treat input validation at trust boundaries, error handling that prevents data loss, security, accessibility, hardware calibration, and anything explicitly requested as never subject to laziness shortcuts. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Leave one runnable check behind for non-trivial logic — the smallest thing that fails if the logic breaks — except for trivial one-liners, which need no test. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## MUST_NOT

- Do not pass a growing collection of positional tuples or unrelated dictionary keys through the application. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Do not use a bare `except`, catch `Exception` to hide a failure, or return `None` as an undocumented substitute for an error. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Resource cleanup must not depend on reference counting, garbage collection, or a caller remembering a separate close operation. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Do not call blocking I/O or CPU-heavy work directly from an event loop. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Do not call `asyncio.run()` from code that may already be running inside an event loop. _[from `com.github.haexmas.atoms.coding-guideline-python/python`, `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Do not claim checks passed, or treat a green-looking diff as verified, without actually running the project's checks and tests; report what was run and any checks that were unavailable or skipped. _[from `com.github.haexmas.atoms.coding-guideline-python/python`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not mock the code under test so heavily that the test only proves its own setup. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Do not use sleeps to wait for concurrency in async tests. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Do not hide unrelated scenarios inside a large fixture or broad parameter matrix. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Do not author changes in the primary checkout; keep it reserved for inspection and worktree management (read-only inspection of any checkout is fine). _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not enforce a universal maximum line count for functions, files, or pull requests; domain complexity, cohesion, control flow, and the number of reasons to change are more meaningful than a line count. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not pad files up to the 500 LoC boundary; it is a maintainability constraint, not a target. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Never add artificial wrappers, blank lines, or extra files only to satisfy a line-count threshold. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not scatter one simple operation across many microscopic functions. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not create folders only for superficial symmetry, such as putting every small class, interface, or helper in its own folder. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not split a file solely to make it shorter. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not place inline test modules or test functions in production files. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not use comments to narrate code that could be made clear through structure and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- LLM agents must not leave references to themselves, their model, runtime, role, prompts, or participation in project artifacts, including commit messages, commit trailers such as `Co-authored-by`, source and documentation comments, changelogs, generated content, metadata, and other repository content. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not use these composed defaults as a reason to reformat or reorganize an entire codebase. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not create an abstraction merely because two things look similar or a change might be reusable — first establish that they share the same behavior, ownership, change reason, and invariants; a small amount of local duplication is preferable to a premature generic abstraction that couples unrelated callers. _[from `com.github.haexmas.atoms.general-coding/general-coding`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Do not add boilerplate nobody asked for. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD

- Prefer precise types and protocols over `Any`, prefer modern built-in generics such as `list[str]` and `dict[str, int]` for supported Python versions, and use abstract collection types such as `Mapping` for read-only inputs when they describe the contract better. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Internal functions should be able to rely on their stated types and invariants rather than repeatedly defending against every possible input. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Prefer simple, readable control flow, using comprehensions for small transformations and a normal loop when conditions, side effects, or error handling would make a comprehension difficult to read. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Library code, or code already inside an event loop, should expose or await a coroutine instead of starting a nested event loop; `asyncio.run()` is appropriate only at an application's top-level entry point. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Avoid adding a new dependency when it can be avoided — prefer the standard library and existing project dependencies, and keep any dependency addition justified by a real capability or maintenance benefit rather than a shorter one-off expression. _[from `com.github.haexmas.atoms.coding-guideline-python/python`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Fixtures should make setup and cleanup explicit. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Use smaller soft thresholds — a function or method beyond roughly 40–60 lines, or nesting deeper than roughly three levels — as prompts for a deliberate review look, not as automatic failures or mandatory limits. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat a change of about 100 net lines as a review-size warning and a change approaching 1000 lines as a strong prompt to split the work or agree on it in advance, identifying generated code, mechanical renames, and required migrations as legitimate exceptions. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer early returns or named helpers when they make the main path easier to follow, and reduce nesting. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- A file should have one primary reason to change; split a file when it has unrelated responsibilities, independent public concepts, different change owners, or tests that are difficult to isolate. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Comments should explain why a non-obvious decision exists, what invariant must be preserved, or which external constraint applies, and should be updated when the behavior changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Separate broad formatting or mechanical refactors from behavior changes when possible, since mixed diffs make review, debugging, rollback, and blame less reliable. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer a change that clearly improves maintainability and correctness over theoretical perfection; when a design is uncertain, make the smallest reversible change that provides evidence about the next decision. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer deletion over addition, boring solutions over clever ones, and the fewest files possible. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Prefer the shortest working diff once the problem is understood; the smallest change in the wrong place is not lazy, it is a second bug. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Question complex requests by asking whether a simpler existing option already covers the need. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD_NOT

- Avoid clever metaprogramming when ordinary functions and classes suffice. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Avoid expensive work, network access, and surprising side effects at module import time unless the project explicitly uses that pattern. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Avoid order dependence, shared mutable module state, real network services, the current time, and arbitrary sleeps in tests. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Avoid re-exporting everything or creating pass-through modules that add no boundary. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Avoid circular imports, hidden global state, and a domain layer that knows about infrastructure details. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_

## MAY

- Generated files, vendored code, snapshots, and other files not normally edited by hand may exceed the 500 LoC boundary, but should be excluded from this assessment. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- A language-required test-module declaration or registration may remain in the production file. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
